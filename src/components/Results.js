const Results = (props) => {
    
    console.log(props)

    // const showData = props.results.map((topalbums artist, idx) => {
    //         return(
    //         < img src={topalbums artist.}
    //         )
    //     }
    // )

    return (
        <>
            <h1>Results</h1>
        </>
    )
}

export default Results;