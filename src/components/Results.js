const Results = (props) => {
    
    console.log(props)

    // const showData = props.results.map((image, idx) => {
    //         return(
    //         < img src={image.}
    //         )
    //     }
    // )

    return (
        <>
            <h1>Results</h1>
        </>
    )
}

export default Results;